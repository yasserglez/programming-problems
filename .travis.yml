language: generic
addons:
  apt:
    packages:
      - python3
      - r-base
      - gcc
      - g++
      - default-jdk
      - scala
before_script:
  - python3 --version
  - Rscript --version
  - gcc --version
  - g++ --version
  - javac -version
  - scalac -version
script: ./run_tests.py
